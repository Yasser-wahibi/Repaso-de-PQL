BEGIN
    FOR I IN (SELECT NOMBRE,NUM_SUP, NUM_DEP
              FROM EMPLEADO
		WHERE PUESTO='ANALISTA') LOOP
        DBMS_OUTPUT.PUT_LINE('NOMBRE: ' ||I.NOMBRE||' ------NUM_SUP: ' || I.NUM_SUP || '----- NUM_DEP: ' || I.NUM_DEP);
    END LOOP;
END;
/
